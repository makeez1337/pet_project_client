const baseURL: string = 'http://localhost:5200';

export enum Urls {
  Auth = '/auth',
  Registration = '/registration',
  Login = '/login',
  Refresh = '/refresh',
  Logout = '/logout'
}

export default baseURL;
